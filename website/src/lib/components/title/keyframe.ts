import { type Keyframe } from "$lib/keyframe.svelte";

let verse = 0;

/**
 * The keyframes for the title.
 */
export const titleKeyframes: Keyframe[] = [
    {
        verse: verse,
        bolded: [],
        toRun: []
    }
];
